# Pi TUI Kit Consumer Floor Plan

## Goal

Raise every active direct consumer of `@narumitw/pi-tui-kit` from `^0.46.0` to the published
`^0.49.1` release, so independently installed consumers receive the runtime-import performance fix.

## Scope

- Package metadata only for the 22 active production and experimental consumers that directly
  import Pi TUI Kit.
- Regenerate the root lockfile with the repository-declared npm 12.0.2 under supported Node 22.
- Do not change consumer behavior, source, public APIs, or package versions.

## Plan

- [x] Verify `@narumitw/pi-tui-kit@0.49.1` and its production/testing exports are available from npm;
      `npm view` under Node 22 reported version 0.49.1, both `.` and `./testing` exports, and registry
      tarball integrity `sha512-RAUz3WsThABg79Ip/3FRmV4RUj0gNMyCirFjHitLUWC5oiZvOWGv1OAquFVWuIoJJEA2MtFf7LZ0OXf3G5UM3g==`.
- [x] Raise all 22 active direct consumer dependency floors to `^0.49.1` and regenerate
      `package-lock.json`; npm 12.0.2 under Node 22.22.2 produced a clean install, all 22 consumers
      deduplicate to Kit 0.49.1, and searches found no active old floor or stale 0.46.0 lock entry.
- [x] Review the metadata diff against `docs/extension-conventions.md`, including direct runtime
      dependency ownership and independent-install compatibility; all touched packages already import
      Kit at runtime and keep it in `dependencies`, and all 22 dry-run packs passed. No settings, UI
      behavior, or extension boundary changed; the full check will run the boundary validator.
- [x] Run the repository CI-equivalent `npm run check` under Node 22/npm 12.0.2; all gates passed with
      2,424/2,424 tests.

## Completion Checklist

- [x] Every active direct consumer declares `"@narumitw/pi-tui-kit": "^0.49.1"` (22/22 verified).
- [x] `package-lock.json` was generated by npm 12.0.2 and contains no stale 0.46.0 Kit resolution for
      active consumers; `npm ci` and `npm ls @narumitw/pi-tui-kit --all` passed under Node 22.22.2.
- [x] All 22 touched packages passed one combined npm 12.0.2 dry-run package inspection covering 429
      published files.
- [x] `npm run check` passed with 2,424/2,424 tests.
- [x] The final diff contains only the 22 intended dependency-floor changes, the regenerated lockfile,
      and this archived plan. Semantic lock audit found no added package records and removed exactly
      the 22 stale nested Kit 0.46.0 records.
